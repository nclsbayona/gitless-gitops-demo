apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "micro-app.fullname" . }}-test-connection"
  labels:
    {{- include "micro-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "micro-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
